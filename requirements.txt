# Core data
pandas>=2.0
openpyxl>=3.1

# GIS / geospatial
geopandas>=0.14
shapely>=2.0
fiona>=1.9

# Optional but useful for the workflow
requests>=2.31

# Reconciliation engine
rapidfuzz>=3.0     # fast fuzzy name/endpoint matching
pyyaml>=6.0        # source manifests (sources/<name>/manifest.yml)
jsonschema>=4.0    # validate manifests against sources/_schema/manifest.schema.json
# pyproj is pulled in transitively by geopandas; used for geodesic length + metric-CRS reprojection
